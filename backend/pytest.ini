[pytest]
testpaths = tests
; Ensures `app` is importable regardless of how pytest is invoked (bare
; `pytest` vs `python -m pytest`) -- without this, whether backend/ ends up
; on sys.path depends on the invocation method, since tests/ has no
; __init__.py of its own for pytest to walk up from.
pythonpath = .
